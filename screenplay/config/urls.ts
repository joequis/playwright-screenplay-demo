export class AppUrls {
  static readonly BASE = 'https://www.saucedemo.com';

  // Posteriores paginas a añadir:
  static readonly LOGIN = `${AppUrls.BASE}/`;
  static readonly INVENTORY = `${AppUrls.BASE}/inventory.html`;
  static readonly CART = `${AppUrls.BASE}/cart.html`;
}
